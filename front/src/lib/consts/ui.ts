import type { PanelSizes, PanelToggle } from "$stores/ui"

export const DEFAULT_PANEL_SIZES: PanelSizes = {
    projectPanel: 20,
    editorPanel: 50,
    resourcePanel: 50,
}
export const DEFAULT_PANEL_TOGGLE: PanelToggle = {
    projectPanel: true,
    editorPanel: true,
    resourcePanel: true,
}