[phases.setup]
nixPkgs = ['rustup', 'openssl', 'cmake']

[phases.install]
cmds = ['rustup default stable']
aptPkgs = ['libssl-dev', 'build-essential']

[phases.build]
cmds = ['make api-build-prod']
dependsOn = ['install']

[start]
cmd = 'make api-prod -o api-build-prod'
